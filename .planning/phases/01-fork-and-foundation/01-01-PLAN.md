---
phase: 01-fork-and-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/mowism/bin/mow-tools.cjs
  - ~/.claude/mowism/bin/mow-tools.test.cjs
autonomous: true
requirements:
  - CORE-02

must_haves:
  truths:
    - "Running `node ~/.claude/mowism/bin/mow-tools.cjs --help` produces output with 'mow-tools' in usage line, no 'gsd' strings"
    - "Running `node ~/.claude/mowism/bin/mow-tools.cjs resolve-model mow-executor` resolves without error"
    - "All 11 agent names in the model profile table use 'mow-' prefix, not 'gsd-'"
    - "Branch templates use 'mow/phase-' and 'mow/{milestone}' prefixes"
    - "Config paths reference '~/.mowism/' not '~/.gsd/'"
  artifacts:
    - path: "~/.claude/mowism/bin/mow-tools.cjs"
      provides: "Core CLI tool for all Mowism workflow operations"
      min_lines: 5000
      contains: "mow-tools"
    - path: "~/.claude/mowism/bin/mow-tools.test.cjs"
      provides: "Test suite for mow-tools.cjs"
      min_lines: 50
      contains: "mow-tools"
  key_links:
    - from: "~/.claude/mowism/bin/mow-tools.cjs"
      to: "model profile table"
      via: "mow-* agent name keys"
      pattern: "mow-(planner|executor|verifier|debugger|codebase-mapper|plan-checker|integration-checker|roadmapper|phase-researcher|project-researcher|research-synthesizer)"
    - from: "~/.claude/mowism/bin/mow-tools.cjs"
      to: "~/.mowism/"
      via: "config directory paths"
      pattern: "\\.mowism"
---

<objective>
Fork `gsd-tools.cjs` and `gsd-tools.test.cjs` from `~/.claude/get-shit-done/bin/` into `~/.claude/mowism/bin/` as `mow-tools.cjs` and `mow-tools.test.cjs`, replacing all 55+ GSD-specific strings with Mowism equivalents.

Purpose: The CLI tool is the foundation — every workflow, command, and agent references it. It must be rebranded first so subsequent plans can reference `mow-tools.cjs` without broken paths.

Output: Two working files in `~/.claude/mowism/bin/` with zero user-visible "gsd" strings.
</objective>

<execution_context>
@/home/max/.claude/get-shit-done/workflows/execute-plan.md
@/home/max/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fork-and-foundation/01-RESEARCH.md

Source files:
@/home/max/.claude/get-shit-done/bin/gsd-tools.cjs
@/home/max/.claude/get-shit-done/bin/gsd-tools.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fork and rebrand mow-tools.cjs</name>
  <files>~/.claude/mowism/bin/mow-tools.cjs</files>
  <action>
    1. Create directory `~/.claude/mowism/bin/` if it does not exist.
    2. Copy `~/.claude/get-shit-done/bin/gsd-tools.cjs` to `~/.claude/mowism/bin/mow-tools.cjs`.
    3. Apply string replacements IN THIS ORDER (most specific first to avoid double-replacement):
       a. `/home/max/.claude/get-shit-done/` -> `/home/max/.claude/mowism/` (absolute paths, ~186 occurrences)
       b. `get-shit-done` -> `mowism` (any remaining path fragments)
       c. `~/.gsd/` -> `~/.mowism/` (config paths)
       d. `.gsd/` -> `.mowism/` (config paths without tilde)
       e. `gsd/phase-` -> `mow/phase-` (branch template)
       f. `gsd/{milestone}` -> `mow/{milestone}` (branch template)
       g. `gsd-tools.cjs` -> `mow-tools.cjs` (self-references)
       h. `gsd-tools` -> `mow-tools` (references without extension)
       i. `gsd-planner` -> `mow-planner`
       j. `gsd-executor` -> `mow-executor`
       k. `gsd-verifier` -> `mow-verifier`
       l. `gsd-phase-researcher` -> `mow-phase-researcher`
       m. `gsd-project-researcher` -> `mow-project-researcher`
       n. `gsd-research-synthesizer` -> `mow-research-synthesizer`
       o. `gsd-debugger` -> `mow-debugger`
       p. `gsd-codebase-mapper` -> `mow-codebase-mapper`
       q. `gsd-plan-checker` -> `mow-plan-checker`
       r. `gsd-integration-checker` -> `mow-integration-checker`
       s. `gsd-roadmapper` -> `mow-roadmapper`
       t. `/gsd:` -> `/mow:` (command name references in error messages, help text)
       u. `GSD ►` -> `MOW ▶` (banner prefix — note different arrow characters: ► vs ▶)
       v. `GSD ▶` -> `MOW ▶` (alternate arrow if present)
       w. Remaining standalone `GSD` in brand contexts (usage line, header comment) -> `MOW`
    4. Update the file header comment from "GSD Tools" to "MOW Tools" and "GSD workflow operations" to "Mowism workflow operations".
    5. Update the temp file prefix: `gsd-${Date.now()}` -> `mow-${Date.now()}`.
    6. Verify: A source-code comment acknowledging GSD origin (e.g., "Forked from GSD") is fine and should NOT be removed. Only user-visible output strings must be rebranded.
  </action>
  <verify>
    Run `grep -c 'gsd' ~/.claude/mowism/bin/mow-tools.cjs` and verify the count is zero or near-zero (only origin comments allowed).
    Run `node ~/.claude/mowism/bin/mow-tools.cjs 2>&1 | head -5` and verify usage line says "mow-tools" not "gsd-tools".
    Run `grep -n 'gsd-planner\|gsd-executor\|gsd-verifier\|gsd-debugger' ~/.claude/mowism/bin/mow-tools.cjs` and verify zero matches.
    Run `grep -n 'get-shit-done' ~/.claude/mowism/bin/mow-tools.cjs` and verify zero matches.
  </verify>
  <done>
    `~/.claude/mowism/bin/mow-tools.cjs` exists, executes without syntax errors, contains zero "gsd"/"get-shit-done" strings in functional code (origin comments excluded), and all agent names, paths, branch templates, and brand strings reference Mowism.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fork and rebrand mow-tools.test.cjs</name>
  <files>~/.claude/mowism/bin/mow-tools.test.cjs</files>
  <action>
    1. Copy `~/.claude/get-shit-done/bin/gsd-tools.test.cjs` to `~/.claude/mowism/bin/mow-tools.test.cjs`.
    2. Apply the SAME ordered replacement table as Task 1 (steps 3a through 3w).
    3. Additionally update any test assertions that check for GSD-branded output strings — these must now assert Mowism-branded output (e.g., test expecting "GSD ►" must now expect "MOW ▶").
    4. Update the require/import path from `gsd-tools.cjs` to `mow-tools.cjs` (the test file loads the tool it tests).
    5. Run the test suite: `node ~/.claude/mowism/bin/mow-tools.test.cjs` and fix any failures caused by the rebranding (e.g., hardcoded assertion strings that still reference "gsd").
  </action>
  <verify>
    Run `node ~/.claude/mowism/bin/mow-tools.test.cjs` — all tests should pass.
    Run `grep -c 'gsd' ~/.claude/mowism/bin/mow-tools.test.cjs` — should be zero or near-zero (origin comments only).
  </verify>
  <done>
    `~/.claude/mowism/bin/mow-tools.test.cjs` exists, all tests pass against `mow-tools.cjs`, and test assertions verify Mowism-branded output rather than GSD-branded output.
  </done>
</task>

</tasks>

<verification>
1. `node ~/.claude/mowism/bin/mow-tools.cjs` runs without crash and shows "mow-tools" in usage
2. `node ~/.claude/mowism/bin/mow-tools.test.cjs` passes all tests
3. `grep -rn 'gsd' ~/.claude/mowism/bin/` returns only origin/attribution comments, never functional code
4. `grep -c 'get-shit-done' ~/.claude/mowism/bin/mow-tools.cjs` returns 0
5. `grep -c 'gsd-executor\|gsd-planner' ~/.claude/mowism/bin/mow-tools.cjs` returns 0
</verification>

<success_criteria>
- mow-tools.cjs executes and produces Mowism-branded output
- mow-tools.test.cjs passes all tests against the rebranded tool
- Zero "gsd"/"get-shit-done" strings in functional code across both files
</success_criteria>

<output>
After completion, create `.planning/phases/01-fork-and-foundation/01-01-SUMMARY.md`
</output>
