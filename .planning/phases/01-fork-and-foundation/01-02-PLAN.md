---
phase: 01-fork-and-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/agents/mow-executor.md
  - ~/.claude/agents/mow-planner.md
  - ~/.claude/agents/mow-verifier.md
  - ~/.claude/agents/mow-phase-researcher.md
  - ~/.claude/agents/mow-project-researcher.md
  - ~/.claude/agents/mow-research-synthesizer.md
  - ~/.claude/agents/mow-debugger.md
  - ~/.claude/agents/mow-codebase-mapper.md
  - ~/.claude/agents/mow-plan-checker.md
  - ~/.claude/agents/mow-integration-checker.md
  - ~/.claude/agents/mow-roadmapper.md
  - ~/.claude/mowism/templates/
  - ~/.claude/mowism/references/
  - ~/.claude/mowism/VERSION
autonomous: true
requirements:
  - CORE-03
  - CORE-04
  - CORE-05

must_haves:
  truths:
    - "All 11 agent definition files exist with 'mow-' prefix names and contain no 'gsd' strings in frontmatter or body"
    - "All template files exist under ~/.claude/mowism/templates/ with Mowism paths and brand strings"
    - "All reference files exist under ~/.claude/mowism/references/ with Mowism paths and brand strings"
    - "The ui-brand.md reference uses 'MOW ▶' prefix, not 'GSD ►'"
  artifacts:
    - path: "~/.claude/agents/mow-executor.md"
      provides: "Executor subagent definition"
      contains: "name: mow-executor"
    - path: "~/.claude/agents/mow-planner.md"
      provides: "Planner subagent definition"
      contains: "name: mow-planner"
    - path: "~/.claude/mowism/templates/config.json"
      provides: "Config template with mow branch templates"
      contains: "mow/phase-"
    - path: "~/.claude/mowism/references/ui-brand.md"
      provides: "UI brand reference with MOW branding"
      contains: "MOW"
    - path: "~/.claude/mowism/VERSION"
      provides: "Version file"
  key_links:
    - from: "~/.claude/agents/mow-*.md"
      to: "~/.claude/mowism/workflows/"
      via: "path references in agent bodies"
      pattern: "/home/max/.claude/mowism/"
    - from: "~/.claude/mowism/templates/config.json"
      to: "branch naming"
      via: "branch template strings"
      pattern: "mow/phase-"
---

<objective>
Fork all GSD agent definitions (11 files), template files (~25 files across 3 directories), reference files (13 files), and the VERSION file into their Mowism locations with full rebranding.

Purpose: These supporting assets are referenced by workflows and commands. Having them in place with correct Mowism paths enables Wave 2 (workflows) to reference them without broken links.

Output: 11 agent files in `~/.claude/agents/mow-*.md`, templates in `~/.claude/mowism/templates/`, references in `~/.claude/mowism/references/`, and `~/.claude/mowism/VERSION`.
</objective>

<execution_context>
@/home/max/.claude/get-shit-done/workflows/execute-plan.md
@/home/max/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-fork-and-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fork and rebrand 11 agent definition files</name>
  <files>
    ~/.claude/agents/mow-executor.md
    ~/.claude/agents/mow-planner.md
    ~/.claude/agents/mow-verifier.md
    ~/.claude/agents/mow-phase-researcher.md
    ~/.claude/agents/mow-project-researcher.md
    ~/.claude/agents/mow-research-synthesizer.md
    ~/.claude/agents/mow-debugger.md
    ~/.claude/agents/mow-codebase-mapper.md
    ~/.claude/agents/mow-plan-checker.md
    ~/.claude/agents/mow-integration-checker.md
    ~/.claude/agents/mow-roadmapper.md
  </files>
  <action>
    For each of the 11 GSD agent files in `~/.claude/agents/gsd-*.md`:
    1. Copy to the corresponding `mow-*.md` name in the same directory.
    2. Apply string replacements in order:
       a. `/home/max/.claude/get-shit-done/` -> `/home/max/.claude/mowism/`
       b. `get-shit-done` -> `mowism`
       c. `gsd-tools.cjs` -> `mow-tools.cjs`
       d. `gsd-tools` -> `mow-tools`
       e. All 11 agent name replacements: `gsd-planner` -> `mow-planner`, `gsd-executor` -> `mow-executor`, etc.
       f. `/gsd:` -> `/mow:` (command references in descriptions)
       g. `GSD ►` -> `MOW ▶` and `GSD ▶` -> `MOW ▶` (banner prefixes)
       h. Standalone `GSD` in brand contexts -> `MOW`
    3. Update YAML frontmatter `name:` field from `gsd-*` to `mow-*`.
    4. Do NOT delete the original `gsd-*.md` files — per locked decision, Mowism coexists with GSD. The originals stay.

    Agent file mapping:
    - gsd-codebase-mapper.md -> mow-codebase-mapper.md
    - gsd-debugger.md -> mow-debugger.md
    - gsd-executor.md -> mow-executor.md
    - gsd-integration-checker.md -> mow-integration-checker.md
    - gsd-phase-researcher.md -> mow-phase-researcher.md
    - gsd-plan-checker.md -> mow-plan-checker.md
    - gsd-planner.md -> mow-planner.md
    - gsd-project-researcher.md -> mow-project-researcher.md
    - gsd-research-synthesizer.md -> mow-research-synthesizer.md
    - gsd-roadmapper.md -> mow-roadmapper.md
    - gsd-verifier.md -> mow-verifier.md
  </action>
  <verify>
    Run `ls ~/.claude/agents/mow-*.md | wc -l` — should return 11.
    Run `grep -l 'gsd' ~/.claude/agents/mow-*.md` — should return no files (no gsd strings in mow agent files).
    Run `grep -c 'name: mow-' ~/.claude/agents/mow-executor.md` — should return 1.
  </verify>
  <done>
    All 11 `mow-*.md` agent files exist in `~/.claude/agents/`, each with correct `name: mow-*` frontmatter and zero "gsd"/"get-shit-done" strings in functional content.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fork and rebrand templates, references, and VERSION</name>
  <files>
    ~/.claude/mowism/templates/ (all files)
    ~/.claude/mowism/references/ (all files)
    ~/.claude/mowism/VERSION
  </files>
  <action>
    **Templates:**
    1. Create `~/.claude/mowism/templates/` directory structure (including `codebase/` and `research-project/` subdirectories).
    2. Copy all files from `~/.claude/get-shit-done/templates/` preserving directory structure.
    3. Apply the full ordered replacement table to every file:
       a. `/home/max/.claude/get-shit-done/` -> `/home/max/.claude/mowism/`
       b. `get-shit-done` -> `mowism`
       c. `~/.gsd/` -> `~/.mowism/`
       d. `.gsd/` -> `.mowism/`
       e. `gsd/phase-` -> `mow/phase-`
       f. `gsd/{milestone}` -> `mow/{milestone}`
       g. `gsd-tools.cjs` -> `mow-tools.cjs`
       h. `gsd-tools` -> `mow-tools`
       i. All 11 agent name replacements
       j. `/gsd:` -> `/mow:`
       k. `GSD ►` -> `MOW ▶`, `GSD ▶` -> `MOW ▶`
       l. Standalone `GSD` in brand contexts -> `MOW`
    4. Pay special attention to `config.json` template — it contains `phase_branch_template` and `milestone_branch_template` with `gsd/` prefix that must become `mow/`.
    5. Pay special attention to `state.md` template — it may contain `/gsd:*` command references that must become `/mow:*`.

    **References:**
    1. Create `~/.claude/mowism/references/` directory.
    2. Copy all 13 files from `~/.claude/get-shit-done/references/`.
    3. Apply the same ordered replacement table.
    4. Pay special attention to `ui-brand.md` — this defines visual patterns. `GSD ►` must become `MOW ▶`, help headers must change from "GSD (Get Shit Done)" to "MOW (Mowism)".
    5. Pay special attention to `model-profiles.md` and `model-profile-resolution.md` — these reference agent names that must use `mow-*` prefix.

    **VERSION:**
    1. Copy `~/.claude/get-shit-done/VERSION` to `~/.claude/mowism/VERSION`.
    2. No string replacements needed in VERSION (it's just a version number).
  </action>
  <verify>
    Run `ls ~/.claude/mowism/templates/ | wc -l` — should match count of source templates directory.
    Run `ls ~/.claude/mowism/references/ | wc -l` — should return 13.
    Run `test -f ~/.claude/mowism/VERSION && echo "exists"` — should say "exists".
    Run `grep -rl 'gsd' ~/.claude/mowism/templates/ ~/.claude/mowism/references/ 2>/dev/null` — should return no files.
    Run `grep 'MOW' ~/.claude/mowism/references/ui-brand.md | head -3` — should show MOW-branded lines.
    Run `grep 'mow/phase-' ~/.claude/mowism/templates/config.json` — should show updated branch template.
  </verify>
  <done>
    All template files exist under `~/.claude/mowism/templates/` (with codebase/ and research-project/ subdirectories), all 13 reference files exist under `~/.claude/mowism/references/`, VERSION file exists, and none contain "gsd"/"get-shit-done" strings in functional content. The ui-brand.md uses "MOW ▶" prefix. The config.json template uses "mow/phase-" branch template.
  </done>
</task>

</tasks>

<verification>
1. `ls ~/.claude/agents/mow-*.md | wc -l` returns 11
2. `ls ~/.claude/mowism/templates/*.md ~/.claude/mowism/templates/*.json 2>/dev/null | wc -l` matches source count
3. `ls ~/.claude/mowism/references/*.md | wc -l` returns 13
4. `grep -rl 'gsd\|get-shit-done' ~/.claude/agents/mow-*.md ~/.claude/mowism/templates/ ~/.claude/mowism/references/ 2>/dev/null` returns empty (no GSD strings)
5. Agent frontmatter names all use `mow-` prefix
</verification>

<success_criteria>
- All 11 agent definitions exist with mow-* names and zero gsd references
- All templates and references exist in ~/.claude/mowism/ with Mowism branding
- VERSION file present
- No "gsd" or "get-shit-done" strings in any functional content
</success_criteria>

<output>
After completion, create `.planning/phases/01-fork-and-foundation/01-02-SUMMARY.md`
</output>
