---
phase: 01-fork-and-foundation
plan: 04
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - ~/.claude/mowism/workflows/ (32 files)
autonomous: true
requirements:
  - CORE-01

must_haves:
  truths:
    - "All 32 workflow files exist under ~/.claude/mowism/workflows/ with Mowism paths, tool references, and brand strings"
    - "Every workflow that references gsd-tools.cjs now references mow-tools.cjs"
    - "Every workflow that spawns subagents references mow-* agent types, not gsd-*"
    - "Every workflow that references /gsd:* commands now references /mow:* commands"
    - "Every workflow that displays banners uses MOW ▶ prefix, not GSD ►"
  artifacts:
    - path: "~/.claude/mowism/workflows/execute-phase.md"
      provides: "Phase execution orchestrator workflow"
      contains: "mow-tools.cjs"
    - path: "~/.claude/mowism/workflows/execute-plan.md"
      provides: "Plan execution workflow"
      contains: "mow-executor"
    - path: "~/.claude/mowism/workflows/new-project.md"
      provides: "Project initialization workflow"
      contains: "/mow:"
    - path: "~/.claude/mowism/workflows/plan-phase.md"
      provides: "Phase planning workflow"
      contains: "mow-planner"
  key_links:
    - from: "~/.claude/mowism/workflows/*.md"
      to: "~/.claude/mowism/bin/mow-tools.cjs"
      via: "node invocations"
      pattern: "node ~/.claude/mowism/bin/mow-tools.cjs"
    - from: "~/.claude/mowism/workflows/*.md"
      to: "~/.claude/agents/mow-*.md"
      via: "subagent type references"
      pattern: "mow-(executor|planner|verifier|debugger|researcher|checker|mapper|roadmapper|synthesizer)"
    - from: "~/.claude/mowism/workflows/*.md"
      to: "~/.claude/mowism/templates/*.md"
      via: "@ file references"
      pattern: "@~/.claude/mowism/templates/"
    - from: "~/.claude/mowism/workflows/*.md"
      to: "~/.claude/mowism/references/*.md"
      via: "@ file references"
      pattern: "@~/.claude/mowism/references/"
---

<objective>
Fork all 32 GSD workflow files from `~/.claude/get-shit-done/workflows/` into `~/.claude/mowism/workflows/` with full rebranding.

Purpose: Workflows contain the core logic of the system — they reference the CLI tool, spawn subagents, display banners, and reference templates/references. This plan depends on Plan 01 (mow-tools.cjs) and Plan 02 (agents, templates, references) being complete so all referenced paths exist.

Output: 32 workflow files in `~/.claude/mowism/workflows/` with zero GSD references.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-fork-and-foundation/01-RESEARCH.md

Prior plan summaries (needed for path references):
@.planning/phases/01-fork-and-foundation/01-01-SUMMARY.md
@.planning/phases/01-fork-and-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fork and rebrand workflow files A-L (first 16 alphabetically)</name>
  <files>
    ~/.claude/mowism/workflows/add-phase.md
    ~/.claude/mowism/workflows/add-todo.md
    ~/.claude/mowism/workflows/audit-milestone.md
    ~/.claude/mowism/workflows/check-todos.md
    ~/.claude/mowism/workflows/cleanup.md
    ~/.claude/mowism/workflows/complete-milestone.md
    ~/.claude/mowism/workflows/diagnose-issues.md
    ~/.claude/mowism/workflows/discovery-phase.md
    ~/.claude/mowism/workflows/discuss-phase.md
    ~/.claude/mowism/workflows/execute-phase.md
    ~/.claude/mowism/workflows/execute-plan.md
    ~/.claude/mowism/workflows/health.md
    ~/.claude/mowism/workflows/help.md
    ~/.claude/mowism/workflows/insert-phase.md
    ~/.claude/mowism/workflows/list-phase-assumptions.md
    ~/.claude/mowism/workflows/map-codebase.md
  </files>
  <action>
    1. Create `~/.claude/mowism/workflows/` directory if it does not exist.
    2. For each of the 16 workflow files listed above, copy from `~/.claude/get-shit-done/workflows/` to `~/.claude/mowism/workflows/`.
    3. Apply the full ordered replacement table to each file:
       a. `~/.claude/get-shit-done/` -> `~/.claude/mowism/` (absolute paths to workflows, templates, references, bin)
       b. `get-shit-done` -> `mowism` (remaining path fragments)
       c. `~/.gsd/` -> `~/.mowism/`
       d. `.gsd/` -> `.mowism/`
       e. `gsd/phase-` -> `mow/phase-`
       f. `gsd/{milestone}` -> `mow/{milestone}`
       g. `gsd-tools.cjs` -> `mow-tools.cjs`
       h. `gsd-tools` -> `mow-tools`
       i. All 11 agent name replacements (gsd-planner -> mow-planner, gsd-executor -> mow-executor, etc.)
       j. `/gsd:` -> `/mow:` (command references)
       k. `GSD ►` -> `MOW ▶`, `GSD ▶` -> `MOW ▶` (banner prefixes)
       l. Standalone `GSD` in brand contexts -> `MOW`

    These workflows are the HEAVIEST consumers of GSD references — they contain tool invocations (`node .../gsd-tools.cjs`), subagent spawns (`type="gsd-executor"`), template references (`@.../templates/...`), command cross-references (`/gsd:verify-work`), and banner output (`GSD ► Stage 1`). Every category must be caught.

    Critical files in this batch:
    - `execute-phase.md`: Orchestrates plan execution, spawns executors, references many other workflows
    - `execute-plan.md`: Core plan execution logic, spawns subagents, references templates
    - `discuss-phase.md`: User interaction workflow, references templates and commands
  </action>
  <verify>
    Run `ls ~/.claude/mowism/workflows/ | wc -l` — should be at least 16.
    Run `grep -l 'gsd' ~/.claude/mowism/workflows/add-phase.md ~/.claude/mowism/workflows/execute-phase.md ~/.claude/mowism/workflows/execute-plan.md 2>/dev/null` — should return no files.
    Run `grep 'mow-tools.cjs' ~/.claude/mowism/workflows/execute-phase.md | head -3` — should show mow-tools references.
    Run `grep 'mow-executor\|mow-planner' ~/.claude/mowism/workflows/execute-plan.md | head -3` — should show mow agent references.
  </verify>
  <done>
    First 16 workflow files exist in `~/.claude/mowism/workflows/` with all GSD references replaced by Mowism equivalents. Tool paths, agent names, command references, template paths, and brand strings all use Mowism naming.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fork and rebrand workflow files M-V (remaining 16 alphabetically)</name>
  <files>
    ~/.claude/mowism/workflows/new-milestone.md
    ~/.claude/mowism/workflows/new-project.md
    ~/.claude/mowism/workflows/pause-work.md
    ~/.claude/mowism/workflows/plan-milestone-gaps.md
    ~/.claude/mowism/workflows/plan-phase.md
    ~/.claude/mowism/workflows/progress.md
    ~/.claude/mowism/workflows/quick.md
    ~/.claude/mowism/workflows/remove-phase.md
    ~/.claude/mowism/workflows/research-phase.md
    ~/.claude/mowism/workflows/resume-project.md
    ~/.claude/mowism/workflows/set-profile.md
    ~/.claude/mowism/workflows/settings.md
    ~/.claude/mowism/workflows/transition.md
    ~/.claude/mowism/workflows/update.md
    ~/.claude/mowism/workflows/verify-phase.md
    ~/.claude/mowism/workflows/verify-work.md
  </files>
  <action>
    1. For each of the remaining 16 workflow files, copy from `~/.claude/get-shit-done/workflows/` to `~/.claude/mowism/workflows/`.
    2. Apply the SAME full ordered replacement table as Task 1 (steps 3a through 3l).

    Critical files in this batch:
    - `new-project.md`: Project initialization, sets up `.planning/` directory, references many templates
    - `plan-phase.md`: Phase planning orchestrator, spawns planner subagent
    - `verify-work.md`: Verification workflow, references verification patterns
    - `resume-project.md`: Resumes work, reads STATE.md
    - `update.md`: Currently runs `npx get-shit-done-cc@latest`. Per research recommendation, replace the update mechanism with a placeholder that advises running `git pull` on the Mowism repo. Full install/update is Phase 3 (DIST-01).

    Special handling for `update.md`:
    - Do NOT just mechanically replace `npx get-shit-done-cc@latest` with `npx mowism@latest` (that package doesn't exist)
    - Instead, replace the update mechanism with instructions to `cd ~/.claude/mowism && git pull`
    - Add a note that full install/update tooling is planned for Phase 3
  </action>
  <verify>
    Run `ls ~/.claude/mowism/workflows/ | wc -l` — should return 32.
    Run `grep -rl 'gsd\|get-shit-done' ~/.claude/mowism/workflows/` — should return no files.
    Run `grep 'mow-planner' ~/.claude/mowism/workflows/plan-phase.md | head -3` — should show mow-planner references.
    Run `grep -v 'npx get-shit-done' ~/.claude/mowism/workflows/update.md | grep -c 'git pull'` — should show the new update mechanism.
  </verify>
  <done>
    All 32 workflow files exist in `~/.claude/mowism/workflows/` with zero GSD references. The update.md workflow uses a git-pull placeholder instead of the GSD npm package. All tool paths, agent names, command references, and brand strings are Mowism-branded.
  </done>
</task>

</tasks>

<verification>
1. `ls ~/.claude/mowism/workflows/ | wc -l` returns 32
2. `grep -rl 'gsd\|get-shit-done' ~/.claude/mowism/workflows/` returns empty
3. Critical workflows (execute-phase, execute-plan, plan-phase, new-project) reference mow-tools.cjs and mow-* agents
4. update.md uses git-pull mechanism, not npm package reference
5. All @ file references point to ~/.claude/mowism/ paths
</verification>

<success_criteria>
- All 32 workflow files forked and rebranded with zero GSD references
- Tool invocations reference mow-tools.cjs at the mowism path
- Subagent spawns reference mow-* agent types
- Command cross-references use /mow:* prefix
- Banner text uses MOW ▶ prefix
- update.md has placeholder update mechanism
</success_criteria>

<output>
After completion, create `.planning/phases/01-fork-and-foundation/01-04-SUMMARY.md`
</output>
